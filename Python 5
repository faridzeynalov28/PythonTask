'''
say = int(input("Eded sayini daxil edin: "))
lst =  []
for i in range (1, say + 1):
    n = int(input("Eded daxil edin: "))
    lst.append(n)

def ebob(a,b):
    while b != 0:
        a, b = b, a%b
    return a

maks = 0
for i in range (0, say):
    for k in range (i + 1, say):
        if ebob(lst[i],lst[k]) >= maks:
            num_1 = lst[i]
            num_2 = lst[k]
            maks = ebob(lst[i],lst[k])
print(f"Ebobu en boyuk cutluk: {num_1}, {num_2}")            
'''
